@rendermode InteractiveServer

<FluentStack Style="background-color: lightgray; height: 100%; padding-left: 20px; padding-right: 20px "
    VerticalAlignment="VerticalAlignment.Center">
    <FluentSelect TOption="Project"
        Id="project-listbox"
        Items="_projects"
        OptionText="@(p => p.Name)"
        Width="400px"
        Placeholder="Select a project/customer..." />    

    <FluentStack HorizontalAlignment="HorizontalAlignment.Right">
        <RadzenButton ButtonStyle="ButtonStyle.Light" Click="@OnPrevButtonClicked"> &lt; Prev</RadzenButton>
        <RadzenButton ButtonStyle="ButtonStyle.Light" Click="@OnNextButtonClicked">Next &gt; </RadzenButton>
    </FluentStack>
</FluentStack>